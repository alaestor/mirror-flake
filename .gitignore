# Ignores every dotfile (.claude/, .serena/, .ruff_cache/, ...), which is
# deliberate but also hides any future .github/, .envrc, or .editorconfig
# until it's explicitly negated with a `!` rule below.
.*
*.old
__*
result
STATUS.html
STATUS.json
# YubiKey plugin identity stub used by mkIsoWriter's postWrite scripts to
# decrypt initrd host-key backups; local runtime state, never committed.
secrets/age_sk.txt
!.gitignore
